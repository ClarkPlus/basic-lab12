version: '3.3'
services:
  personal-website:
    build: ./myweb
    ports:
      - "8080:80"
    restart: unless-stopped

  todo-app:
    image: prologic/todo:latest
    volumes:
      - todo-data:/go/src/todo/todo.db
    ports:
      - "8081:8000"
    restart: unless-stopped
volumes:
  todo-data: